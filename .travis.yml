language: rust
rust:
  - stable
  - beta
  - nightly
matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true
script:
  - cargo build --verbose --all
  - cargo test --verbose --all
jobs:
  include:
    -stage: compile linux assets and publish
    os: linux
    deploy:
      provider: releases
      api_key: $GITHUB_TOKEN
      file: target/release/clipboard2json.sh
      skip_cleanup: true
